# PersonalParakeet v3 Dependencies
# Core Flet UI framework
flet>=0.21.0

# Audio processing
sounddevice>=0.4.6
numpy>=1.24.0
scipy>=1.10.0
soundfile>=0.12.0

# Optional: Alternative audio (fallback)
pyaudio>=0.2.11

# Configuration and utilities
python-dotenv>=1.0.0
dataclasses-json>=0.6.0

# Text injection and control
keyboard>=0.13.5
pynput>=1.8.1
pyperclip>=1.9.0

# ML dependencies moved to Conda (see environment.yml)
# This provides better CUDA compatibility and easier GPU management
# To install ML dependencies: conda env create -f environment.yml -n personalparakeet

# Development and testing
pytest>=7.0.0
pytest-asyncio>=0.21.0